# FluxCast Python SDK

`fluxcast.fcdp` is a dependency-free, strict FCDP v0.1 framing module. It is
currently a packet framing SDK, not a complete secure publishing SDK: use the
Rust security transport until the signed handshake has stable cross-language
test vectors.
