# FluxCast Go SDK

Strict, dependency-free FCDP v0.1 packet framing. Secure-session interoperability
is not yet claimed until cross-language handshake vectors are published.

`go test ./...` verifies this module against the canonical packet vectors in
the repository specification.
